<resources>
<!---->
<!---->
    <style name="AppCompatTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="android:homeAsUpIndicator">@null</item>
        <item name="colorPrimary">@color/primary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">@color/primarynotification</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:textColorPrimary">@color/textColor</item>
        <item name="android:windowBackground">@color/windowBackground</item>
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/MyActionBarTabText</item>
        <item name="android:actionMenuTextColor">@color/white</item>
        <item name="android:logo">@android:color/transparent</item>

    </style>

    <!-- ActionBar styles -->
    <style name="AppCompatTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppCompatTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark" >
        <item name="android:actionBarItemBackground">?android:selectableItemBackgroundBorderless</item>
        <item name="contentInsetStart">0dp</item>
        <!--<item name="android:contentInsetStart">0dp</item>-->
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>